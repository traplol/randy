
#include syscall
#include string
#include file

proc exit code do
    syscall(SYS_exit, code);
end

extern puts cstr;
extern printf fmt, ...;
extern fprintf file, fmt, ...;
extern putchar char;
extern abort;

extern stdin;
extern stdout;
extern stderr;