
#include syscall
#include file

extern puts cstr;
extern printf fmt, ...;
extern fprintf file, fmt, ...;
extern fflush file;
extern putchar char;
extern abort;

extern stdin;
extern stdout;
extern stderr;

const true = 1;
const false = 0;
const NULL = 0;
const sizeof_PTR = 8;


proc exit code in
    fflush(u64@(stdout));
    fflush(u64@(stderr));
    syscall(SYS_exit, code);
end